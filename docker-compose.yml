services:
  nova-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      # Production environment configuration
      - ASPNETCORE_ENVIRONMENT=Production
    # Comment out local postgres dependency since we're using external Supabase
    # depends_on:
    #   - postgres
    networks:
      - nova-network

  # Comment out local postgres since we're using Supabase in production
  # postgres:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: nova_local
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - nova-network

volumes:
  postgres_data:

networks:
  nova-network:
    driver: bridge